
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile in the Playmakers app, storing personal details, friend connections, and general account information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "username": {
          "type": "string",
          "description": "The user's chosen unique username for display within the app."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, used for communication and account recovery.",
          "format": "email"
        },
        "profilePictureUrl": {
          "type": "string",
          "description": "URL to the user's profile picture, if set.",
          "format": "uri"
        },
        "bio": {
          "type": "string",
          "description": "A short biographical description provided by the user for their profile."
        },
        "friendIds": {
          "type": "array",
          "description": "References to other UserProfile entities that this user has added as friends.",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was last updated.",
          "format": "date-time"
        },
        "role": {
          "type": "string",
          "description": "The user's role, e.g., 'admin' or 'player'.",
          "enum": ["admin", "player"]
        }
      },
      "required": [
        "id",
        "username",
        "email",
        "createdAt",
        "updatedAt"
      ]
    },
    "Game": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Game",
      "type": "object",
      "description": "Represents a friend-to-friend sweepstakes or pick-em game created by a user for a specific sports event.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Game entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the game."
        },
        "creatorId": {
          "type": "string",
          "description": "Reference to the UserProfile entity of the user who created this game."
        },
        "creatorPick": {
          "type": "string",
          "description": "The team or athlete selected by the creator."
        },
        "opponentId": {
          "type": "string",
          "description": "Reference to the UserProfile entity of the user who accepted the challenge."
        },
        "opponentPick": {
          "type": "string",
          "description": "The team or athlete selected by the opponent."
        },
        "sportId": {
          "type": "string",
          "description": "The sport this game is based on."
        },
        "currencyType": {
          "type": "string",
          "description": "The currency used for entry: 'gold' or 'sweeps'."
        },
        "entryFee": {
          "type": "number",
          "description": "The number of coins required to enter."
        },
        "prizePool": {
          "type": "number",
          "description": "The total potential coins awarded to winners."
        },
        "status": {
          "type": "string",
          "description": "The current status (e.g., 'Open', 'Live', 'Completed')."
        },
        "inviteCode": {
          "type": "string",
          "description": "A unique code that friends can use to join."
        },
        "winnerId": {
          "type": "string",
          "description": "Reference to the UserProfile of the winner."
        },
        "finalScores": {
          "type": "object",
          "description": "Final scores for each participant.",
          "additionalProperties": { "type": "number" }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the game was created.",
          "format": "date-time"
        }
      },
      "required": [
        "name",
        "creatorId",
        "sportId",
        "currencyType",
        "entryFee",
        "status",
        "createdAt"
      ]
    },
    "UserWallet": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserWallet",
      "type": "object",
      "description": "Manages the in-app currency balances (Gold Coins and Sweepstakes Coins) for each user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserWallet entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the UserProfile entity this wallet belongs to."
        },
        "goldCoins": {
          "type": "number",
          "description": "The current balance of Gold Coins."
        },
        "sweepstakesCoins": {
          "type": "number",
          "description": "The current balance of Sweepstakes Coins."
        },
        "lastUpdated": {
          "type": "string",
          "description": "Timestamp indicating when the wallet balances were last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "goldCoins",
        "sweepstakesCoins",
        "lastUpdated"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/wallet",
        "definition": {
          "entityName": "UserWallet",
          "schema": {
            "$ref": "#/backend/entities/UserWallet"
          },
          "description": "Manages in-app currency balances for each user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique ID of the user."
            }
          ]
        }
      },
      {
        "path": "/games/{gameId}",
        "definition": {
          "entityName": "Game",
          "schema": {
            "$ref": "#/backend/entities/Game"
          },
          "description": "Represents a friend-to-friend game arena.",
          "params": [
            {
              "name": "gameId",
              "description": "The unique ID of the game."
            }
          ]
        }
      }
    ]
  }
}
